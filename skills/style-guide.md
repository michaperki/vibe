# Skill: Style Guide (JS/HTML/CSS)

- Keep changes minimal and focused; prefer unified diffs.
- Follow existing naming and structure; avoid large refactors.
- Use semantic HTML and accessible labels.
- Prefer small utilities; avoid heavy dependencies.
- Preserve Windows/PowerShell friendliness in docs/commands.
